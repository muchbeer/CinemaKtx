package raum.muchbeer.cinemaktx.data.db

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query
import raum.muchbeer.cinemaktx.data.model.ArtistList
import raum.muchbeer.cinemaktx.data.model.ArtistModel

//@Dao
interface ArtistDao {

/*
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertActorsList(actorsList: List<ArtistList>)

    @Query("DELETE FROM artist_table")
    suspend fun deleteAllArtist()

    @Query("SELECT * FROM artist_table")
    suspend fun retrieveArtistShow(artist: List<ArtistModel>)
*/

}